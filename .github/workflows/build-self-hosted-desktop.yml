name: Build self-hosted desktop packages

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: self-hosted-desktop-${{ github.ref }}
  cancel-in-progress: true

env:
  BUILD_CHANNEL: stable
  FLUXER_SELF_HOSTED: "true"
  PUBLIC_RELEASE_CHANNEL: stable
  PUBLIC_BUILD_VERSION: selfhost-${{ github.run_number }}

jobs:
  windows:
    name: Windows x64 installer
    runs-on: windows-2025
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.93.0"
          targets: x86_64-pc-windows-msvc

      - name: Set up pnpm
        shell: pwsh
        run: |
          corepack enable
          corepack prepare pnpm@10.29.3 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build desktop app
        working-directory: fluxer_desktop
        env:
          ELECTRON_ARCH: x64
        run: pnpm build

      - name: Package Windows app
        working-directory: fluxer_desktop
        env:
          ELECTRON_ARCH: x64
        run: pnpm exec electron-builder --config electron-builder.config.cjs --win nsis --x64 --publish never

      - name: Upload Windows package
        uses: actions/upload-artifact@v6
        with:
          name: Fluxer-Windows-x64
          path: fluxer_desktop/dist-electron/Fluxer-Setup-*.exe
          if-no-files-found: error
          retention-days: 30

  macos:
    name: macOS Apple Silicon DMG
    runs-on: macos-15
    timeout-minutes: 150
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Set up Python packaging support
        run: python3 -m pip install --break-system-packages setuptools

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.93.0"
          targets: aarch64-apple-darwin

      - name: Set up pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.29.3 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build desktop app
        working-directory: fluxer_desktop
        env:
          ELECTRON_ARCH: arm64
          FLUXER_MAC_UNSIGNED: "true"
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: pnpm build

      - name: Package Apple Silicon macOS app
        working-directory: fluxer_desktop
        env:
          ELECTRON_ARCH: arm64
          FLUXER_MAC_UNSIGNED: "true"
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: pnpm exec electron-builder --config electron-builder.config.cjs --mac dmg --arm64 --publish never

      - name: Upload macOS packages
        uses: actions/upload-artifact@v6
        with:
          name: Fluxer-macOS-Apple-Silicon
          path: fluxer_desktop/dist-electron/*.dmg
          if-no-files-found: error
          retention-days: 30
